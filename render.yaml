services:
  - type: web
    name: incidentmind-api
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: INCIDENTMIND_REPORT_DIR
        value: /tmp/incident_reports
